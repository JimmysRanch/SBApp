// PostCSS configuration for Tailwind CSS and Autoprefixer.  Because this
// project is configured as an ES module (see package.json), we use
// an ES export rather than CommonJS module.exports.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};